# snap-capsule 协作约定

> 记录用户（LeonidasLux）在本仓库协作中的习惯与偏好，供后续会话遵守。

## 沟通

- **始终使用简体中文回复**（与全局约定一致）。
- 结论尽量直接、给证据；不要堆叠反问或把问题抛回给用户做无谓自查。

## 排查与定位

- 改动或缺陷报告，**先自查本项目代码与产物**（shared / H5 bundle），确认无误后再考虑浏览器缓存、端口、环境等外部因素。
- 用户明确陈述过的事实（如“我已强制刷新”“就是 8080”）**直接采信**，不要再归因到用户操作；转而从代码侧复核根因。
- 出问题时给修复 + 实证，而不是只给排查步骤让用户反复尝试。

## 验证习惯

- **验证执行权归用户**：改动完成后由用户亲手在真机/页面验证，Claude 负责**讲清怎么验证**——给出可直接照做的操作路径（点哪、滑哪、看什么）、预期结果，以及核对落盘数据的方法；不擅自长时间占用模拟器做截图/OCR 驱动验证（本环境截图文字不可靠）。确需自证时先在代码侧与产物侧给证据。
- 修改要**真实跑通**：在页面里点击交互、核对产物/落盘数据，不满足于“代码看起来对”。
- 复核时间/判定逻辑时，**照抄实现计算**，不要按注释措辞或“我以为的规则”去复刻预期；当心把 epoch 毫秒当成“人读的 天/小时”（本项目曾把 `now - 6L`（6 毫秒）误当 6 天而漏判）。
- 注意本项目仍有两把时钟：数据 createdAt 按“生成时刻”冻结；卡片脚注的相对时间按“进入主页时取到的 now”（`remember { CapsuleStore.now() }`）计算，两者勿混。v2 起主页只有「未完成 / 已完成」两枚 Tab，已无「今天 / 近一周」时间窗口筛选。

## 示例/演示数据偏好

- 示例数据宜**内容丰富且贴近真实**：规模够大（本仓库示例为 50 条）、时间**横跨三年**；含已完成（便于演示「已完成」Tab 的置灰与“移回未完成”）、含少量回收站（便于演示恢复/彻底删除）；让 未完成 / 已完成 / 回收站 三个视图都有内容可看（v2 起无归档箱与时间分组）。
