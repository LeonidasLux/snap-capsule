# snap-capsule 协作约定

> 记录用户（LeonidasLux）在本仓库协作中的习惯与偏好，供后续会话遵守。

## 沟通

- **始终使用简体中文回复**（与全局约定一致）。
- 结论尽量直接、给证据；不要堆叠反问或把问题抛回给用户做无谓自查。

## 排查与定位

- 改动或缺陷报告，**先自查本项目代码与产物**（shared / H5 bundle），确认无误后再考虑浏览器缓存、端口、环境等外部因素。
- 用户明确陈述过的事实（如“我已强制刷新”“就是 8080”）**直接采信**，不要再归因到用户操作；转而从代码侧复核根因。
- 出问题时给修复 + 实证，而不是只给排查步骤让用户反复尝试。

## 验证习惯

- **验证执行权归用户**：改动完成后由用户亲手在真机/页面验证，Claude 负责**讲清怎么验证**——给出可直接照做的操作路径（点哪、滑哪、看什么）、预期结果，以及核对落盘数据的方法；不擅自长时间占用模拟器做截图/OCR 驱动验证（本环境截图文字不可靠）。确需自证时先在代码侧与产物侧给证据。
- 修改要**真实跑通**：在页面里点击交互、核对产物/落盘数据，不满足于“代码看起来对”。
- 复核时间/筛选/窗口等判定逻辑时，**照抄实现计算**，不要按注释措辞或“我以为的规则”去复刻预期（本项目曾因把 `now - 6L`（6 毫秒）当成 6 天、且按“正确意图”而非实现去验证而漏判「近一周」=「今天」的 bug）。
- 注意本项目有两把时钟：数据 createdAt 按“生成/载入时刻”冻结，界面「今天 / 近一周」窗口按“当前查看时刻”滑动，两者勿混。

## 示例/演示数据偏好

- 示例数据宜**内容丰富且贴近真实**：规模够大（本仓库示例为 50 条）、时间**横跨三年**、含少量归档（便于演示归档箱）、分组覆盖 今天 / 近一周 / 近一月 / … / 一年以上，让各筛选视图都有内容可看。
